import { Outlet, Link } from "react-router-dom";


// BURAYI ELLEME BURA NAVBAR VE FOOTER

const Layout = () => {
    return (
        <div>
            
            <Outlet />

        </div>
    )
};

export default Layout;